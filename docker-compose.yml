version: '3.8'

services:
  # MongoDB local comentado - ahora usamos Atlas
  # mongo:
  #   image: mongo:latest
  #   ports:
  #     - "27017:27017"
  #   volumes:
  #     - mongo-data:/data/db
  #   networks:
  #     - app-network

  backend:
    build:
      context: .
      dockerfile: server/Dockerfile
    ports:
      - "9002:9002"
    environment:
      # MongoDB Atlas Connection
      - MONGO_URI=mongodb+srv://yesiwindecker:yeyu2025@bolucompras.si6oltd.mongodb.net/bolucompras?retryWrites=true&w=majority&appName=Bolucompras
      - PORT=9002
    networks:
      - app-network

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - BACKEND_URL=http://backend:9002
    ports:
      - "3000:3000"
    environment:
      - BACKEND_URL=http://backend:9002
    depends_on:
      - backend
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  mongo-data:
